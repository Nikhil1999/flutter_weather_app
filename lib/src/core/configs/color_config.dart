import 'package:flutter/material.dart';

/// Contents all colors related to app
class ColorConfig {
  const ColorConfig._();

  static const Color black = Colors.black;
  static const Color white = Colors.white;

  static const Color primary = Color(0xFF5171EC);
  static const Color primaryL1 = Color(0xFF61A0F1);
}
